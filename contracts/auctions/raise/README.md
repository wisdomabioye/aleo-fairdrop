# fairdrop_raise.aleo

Fixed-price fundraise. Participants commit credits at a fixed price per token; the auction succeeds when `supply` is fully committed before `end_block`. If the target is not met, all participants are refunded (soft cap).

No price discovery — simpler than Dutch for projects with a known target raise and token price.

## Status

Design complete. Implementation pending.
