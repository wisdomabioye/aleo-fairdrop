# @fairdrop/config

Environment-specific runtime constants — network URLs, program IDs per network, feature flags. Consumed by both `apps/web` and the services.

Throws at import time if any required env variable is missing, so misconfigured deployments fail loudly at startup rather than at runtime.

## Status

Not yet implemented.
